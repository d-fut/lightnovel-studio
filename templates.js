// ===== Prompt Templates =====

const DEFAULT_TEMPLATES = {
  concept: {
    name: '01 コンセプト・世界観',
    system: `あなたはSFコンセプトデザイナーです。日本のラノベ市場に精通し、独創的かつ市場性のある世界観を設計します。`,
    user: `以下の条件でラノベの世界観コンセプトを正確に10案生成せよ。

## 基本情報
- 総字数目標：{total_chars}字
- ジャンル：{genre}
- テーマキーワード：{theme}

## よく使われる設定例（参考・差別化に使え）
異世界転生、チート能力、魔法学校、ハーレム、ギルド、勇者・魔王、VRゲーム世界、タイムループ、記憶喪失、契約精霊

## 禁止：上記の設定をそのまま使うこと

## 出力フォーマット（Markdownのみ・JSONは使わない）
10案を以下の形式で繰り返せ。前置き・説明・コードブロック不要。

---
### 案N ｜ スコア：X/10

**キャッチコピー：**（30字以内）
**世界の核心ルール：**（3行以内）
**主人公が直面する問い：**（1文）
**差別化ポイント：**（1文）
**スコア根拠：**（1文）

---
（案ごとに繰り返す）

## 制約
- 設定のための設定にしない
- 「問い」は読者が自分事にできるものに限定
- スコアは客観的に評価し、8点以上は最大2案まで
- Markdownのみ返せ。JSONは使わない`
  },

  plot: {
    name: '02 プロット構築',
    system: `あなたはプロ脚本家です。三幕構成でプロットを設計します。感情的煽りより論理的必然性を重視します。`,
    user: `以下のコンセプトから三幕構成プロットを設計せよ。

## コンセプト決定稿
{concept}

## 基本情報
- 総字数目標：{total_chars}字
- ジャンル：{genre}
- テーマ：{theme}
- 固有名詞リスト：{nouns}

## 各幕の字数配分（合計が総字数と一致すること）
- 第一幕（序）：{act1_chars}字（総字数の{act1_pct}%）
- 第二幕（破）：{act2_chars}字（総字数の{act2_pct}%）
- 第三幕（急）：{act3_chars}字（総字数の{act3_pct}%）
- クライマックス位置：{climax_pos}字地点（第二幕終盤〜第三幕冒頭）

## 出力フォーマット（Markdownのみ・JSONは使わない）
各幕を以下の見出し構造で出力せよ。前置き・説明・コードブロック不要。

### 第一幕（序）｜{act1_chars}字
**日常と欠如：**（ここに記述）
**事件（引き金）：**（ここに記述）
**伏線：**（最低1本、ここに記述）

### 第二幕（破）｜{act2_chars}字
**主人公の選択と代償：**（ここに記述）
**中間点（価値観の転換）：**（ここに記述）
**最大の危機：**（ここに記述）

### 第三幕（急）｜{act3_chars}字
**クライマックス（{climax_pos}字地点・論理的決断として）：**（ここに記述）
**結末と余韻：**（ここに記述）

## 制約
- 感動を「泣ける」で表現しない
- クライマックスは総字数の70〜80%地点
- 伏線は第一幕で必ず1本以上
- Markdownのみ返せ。JSONは使わない`
  },

  characters: {
    name: '03 キャラクター設計',
    system: `あなたはキャラクター設計の専門家です。アンサンブルの一貫性とテーマとの連動を重視します。`,
    user: `以下の情報から登場人物を設計せよ。

## プロット
{plot}

## テーマ
{theme}

## 固有名詞リスト（既存）
{nouns}

## 出力フォーマット（Markdownのみ・JSONは使わない）
各キャラクターを以下の見出し構造で出力せよ。前置き不要。

---
## キャラクター名（年齢・役職）

**表の動機：**（他者への説明）
**裏の動機：**（本人も認識が薄い）
**口癖・言動の癖：**（具体的に）
**主人公との対比：**（対比構造）
**絶対に言わないセリフ：**
- （例1）
- （例2）

---
（キャラクターごとに繰り返す）

最後に以下を付記：
## アンサンブル評価
（全キャラがテーマへの答えのバリエーションになっているか評価）

## 制約
- 「論理的な外面 × 感情的な内核」の構造を全キャラに持たせる
- 主人公・ライバル・協力者・鍵キャラの4名以上
- Markdownのみ返せ。JSONは使わない`
  },

  chapters: {
    name: '04 章立て・構成',
    system: `あなたは構成編集者です。読者を飽きさせない章構成を設計します。`,
    user: `以下のプロットから章構成表を作成せよ。

## プロット
{plot}

## 総字数目標
{total_chars}字

## 固有名詞リスト
{nouns}

## 出力フォーマット（Markdownのみ・JSONは使わない）
章ごとに以下の構造で出力せよ。

---
### 第N章「タイトル」｜目安X字

**視点人物：**（名前）
**主な出来事：**（内容）
**冒頭の引き：**（内容）
**末尾フック：**（内容）【疑問 / 誤解 / 選択】

---
（章ごとに繰り返す）

## 制約
- 各章は単独で読んでも「問い」が残る構造
- 連続する2章で同じ感情トーンを使わない
- 章末フックは必ず「疑問」「誤解」「選択」のいずれかに分類して【】内に記載
- 総字数÷1章あたり2000〜3000字で章数を計算
- Markdownのみ返せ。JSONは使わない`
  },

  draft: {
    name: '05 章下書き',
    system: `あなたは日本のラノベ作家です。読者を引き込む文章を書きます。感情の直接描写を避け、描写と行動で感情を表現します。`,
    user: `以下の仕様でこの章の本文を生成せよ。

## 章仕様
{chapter_spec}

## コンセプト・テーマ
{theme}

## 固有名詞リスト
{nouns}

## これまでの章要約
{summaries}

{style_section}

## 禁止表現リスト
- 「胸が痛んだ」「心が震えた」「感情が高ぶった」等の感情直接描写
- 「〜のだった」調の冗長な締め
- 比喩の連続使用（1段落1個まで）
- キャラクターの独白での自己説明
- 「まるで〜のような〜のような」の入れ子比喩

## 出力
{target_chars}字程度。会話・描写・内省のバランスを均等に。
本文のみ出力し、末尾に自己チェックを付記：
[ ] 禁止表現なし
[ ] 末尾フック達成：{ending_hook}
[ ] 固有名詞の表記一致`
  },

  consistency: {
    name: '06 整合性チェック',
    system: `あなたは厳格な編集者です。感情的評価を一切せず、論理的矛盾のみを報告します。`,
    user: `以下の原稿を整合性チェックせよ。

## 固有名詞リスト
{nouns}

## キャラクター設計
{characters}

## 原稿（要約版）
{manuscript}

## チェック項目
1. 時系列矛盾（日時・年齢・経過時間）
2. キャラクター行動の動機一貫性
3. 世界観ルールの逸脱
4. 伏線の回収漏れ
5. 固有名詞の表記ゆれ

## 出力フォーマット（Markdownのみ・JSONは使わない）
以下の構造で出力せよ。問題なしの項目も必ず記載。

## 整合性チェック結果

### 時系列矛盾
（問題ありの場合）**箇所：**… **内容：**… **修正案：**…
（問題なしの場合）問題なし

### キャラクター動機の一貫性
（同上）

### 世界観ルールの逸脱
（同上）

### 伏線の回収漏れ
（同上）

### 固有名詞の表記ゆれ
（同上）

感想・評価・褒め言葉は一切不要。Markdownのみ返せ。JSONは使わない。`
  },

  revision: {
    name: '07 文体統一・改稿',
    system: `あなたは文体統一の専門家です。指定されたスタイルガイドに厳密に従います。`,
    user: `以下の本文をスタイルガイドに従いリライトせよ。

## スタイルガイド
- 文末バリエーション：「〜た」「〜る」「〜だ」を3:2:1の比率
- 一文の最大字数：60字
- 会話文の直後に必ず1行の非言語描写
- 同一語句の1段落内重複禁止

## トーン
{tone}

## 原稿
{manuscript}

リライト後の本文のみ出力。
末尾に変更サマリーを3行以内で付記。`
  },

  polish: {
    name: '08 最終仕上げ',
    system: `あなたは校正専門家です。誤字脱字・文法・一貫性を厳密にチェックします。`,
    user: `以下の本文を校正せよ。

## 固有名詞リスト（表記の正解）
{nouns}

## 原稿
{manuscript}

## チェック（優先順位順）
1. 誤字脱字・送り仮名
2. 読点の過不足（一文に3個以上は分割）
3. 同一語句の3行以内重複
4. セリフの語尾キャラクター一貫性

冒頭に「修正N件」と記載後、修正済み本文を出力。`
  },

  visual_character: {
    name: 'VIS キャラビジュアル',
    system: `あなたは画像生成AIのプロンプトエンジニアです。`,
    user: `以下のキャラクター設定から画像生成プロンプトを作成せよ。

## キャラクター情報
{character}

## 画風スタイル
{style}

## 出力フォーマット（Markdownのみ・JSONは使わない）

## キャラクタービジュアルプロンプト

### Stable Diffusion
\`\`\`
（SD形式プロンプト）
\`\`\`

### NovelAI
\`\`\`
（NAI形式プロンプト）
\`\`\`

### Midjourney
\`\`\`
/imagine prompt: （MJ形式プロンプト）
\`\`\`

### ネガティブプロンプト（共通）
\`\`\`
（ネガティブプロンプト）
\`\`\`

Markdownのみ返せ。JSONは使わない。`
  },

  visual_scene: {
    name: 'VIS シーン挿絵',
    system: `あなたは画像生成AIのプロンプトエンジニアです。`,
    user: `以下の情報から最も盛り上がるシーンの挿絵プロンプトを作成せよ。

## プロット・クライマックス
{climax}

## 登場キャラクター
{characters}

## 画風スタイル
{style}

## 出力フォーマット（Markdownのみ・JSONは使わない）

## シーン挿絵プロンプト

**シーン説明：**（日本語で簡潔に）

### Stable Diffusion
\`\`\`
（SD形式プロンプト）
\`\`\`

### NovelAI
\`\`\`
（NAI形式プロンプト）
\`\`\`

### Midjourney
\`\`\`
/imagine prompt: （MJ形式プロンプト）
\`\`\`

### ネガティブプロンプト（共通）
\`\`\`
（ネガティブプロンプト）
\`\`\`

Markdownのみ返せ。JSONは使わない。`
  },

  summary: {
    name: '章要約（自動）',
    system: `あなたは編集者です。章の要約を正確に作成します。`,
    user: `以下の章本文を3〜5行で要約せよ。
キャラクターの行動・判断・変化のみを記述。感想・評価不要。

## 本文
{draft}

要約のみ出力（前置き不要）。`
  }
};

// Custom templates (stored in IndexedDB)
const TemplateManager = {
  _templates: null,

  async load() {
    if (this._templates) return this._templates;
    const saved = await dbGet('custom_templates');
    this._templates = saved || {};
    return this._templates;
  },

  async get(key) {
    const saved = await this.load();
    return saved[key] || DEFAULT_TEMPLATES[key] || null;
  },

  async set(key, template) {
    const saved = await this.load();
    saved[key] = template;
    await dbSet('custom_templates', saved);
  },

  async reset(key) {
    const saved = await this.load();
    delete saved[key];
    await dbSet('custom_templates', saved);
  },

  getDefault(key) {
    return DEFAULT_TEMPLATES[key] || null;
  },

  getAllKeys() {
    return Object.keys(DEFAULT_TEMPLATES);
  }
};

// Build prompt with variable substitution
function buildPrompt(template, vars) {
  let text = template;
  for (const [k, v] of Object.entries(vars)) {
    text = text.replaceAll(`{${k}}`, v || '（未設定）');
  }
  return text;
}
